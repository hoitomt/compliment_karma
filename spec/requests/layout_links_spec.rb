require 'spec_helper'

describe "LayoutLinks" do
  
  before(:each) do
    reset_session
  end

  describe "If not signed in as founder" do
    
    # it "should redirect when requesting '/'" do
    #   get '/'
    #   response.should redirect_to(new_shell_path)
    # end
    # 
    # it "should redirect when requesting 'signup'" do
    #   get '/signup'
    #   response.should redirect_to(new_shell_path)      
    # end
    # 
    # it "should redirect when requesting 'signin'" do
    #   get '/signin'
    #   response.should redirect_to(new_shell_path)
    # end
  end
  
end
