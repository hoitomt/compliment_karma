// update popup-content
var listId = 'li#<%= @group.id %>';
console.log(listId);
var container = $(listId).find('.popup');
console.log(container);
var content = '<%=j render :partial => "users/privacy/popup",
													 :locals => {:groups => @groups,
													 						 :subject_group => @group} %>';
$(container).html(content);

var groupList = '<%= super_group_list(@group) %>';
var csvList = $(listId).find('#csv-list-groups');
csvList.html(groupList);