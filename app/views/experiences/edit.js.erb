var view = '<%= j(render :partial => "inline_forms/experience", 
												 :locals => {:user => @user, 
												 						 :experience => @experience} ) %>';
var options = {
  view: view,
  width: 650
};
ShowFancybox.init(options);
InlineForms.init();