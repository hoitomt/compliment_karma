var view = '<%=j render "contacts/new_contact_form" %>';

$.fancybox(view, {
	beforeLoad: function() {
		$('body').addClass('lock-screen');
		SiteSearch.resetContactSearch();
	},
	beforeClose: function() {
		$('body').removeClass('lock-screen');
	},
	maxWidth: 800,
	padding: 0,
	wrapCSS: 'background-color: #A7DBD8;',
	helpers: {
		title: null
	}
});

var searchField = $('#new-contact-form').find('#search_string');
var siteSearchContact = new SiteSearch(searchField, 'new-contact');
$(searchField).focus();
