var id = '<%= "#{@recognition_type_id}_#{@recognition_id}" %>';
var commentsCount = '<%= @comments_count %>';
var updateId = "#comment-count-" + id;
var containerId = '#feed-item-social-' + id;
var newComment = '<%=j render "recognition_comments/comment_list", :comments => @comments %>';

$('#detail-comments-list-js').hide().html(newComment).fadeIn(1000);
UserProfile.hideCommentSubmitButton();
UserProfile.clearComment();

$(updateId).html("Comment(" + commentsCount + ")");

<% unless flash[:recognition_comment_errors].blank? %>
	var errors = "<%= flash[:recognition_comment_errors] %>"
	$.jGrowl(errors);
<% end %>
